class Game {
  constructor() {}

  getState() {
    var getStateRef = database.ref('gameState');
    getStateref.on('value, function (')
  }

  start() {
    form = new Form();
    form.display();
    player = new Player();
  }
}
